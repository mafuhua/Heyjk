<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yuenkeji.heyjk" >

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.RESTART_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".activity.CurveActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.Dialog" >
        </activity>

        <service
            android:name=".viewpager.homesubPagers.BluetoothLeService"
            android:enabled="true" />
        <service
            android:name=".viewpager.homesubPagers.blood.BloodService"
            android:enabled="true" />
        <service
            android:name=".viewpager.homesubPagers.weight.WeightService"
            android:enabled="true">
        </service>
        <service android:name=".viewpager.homesubPagers.weight.BlueService"/>
    </application>

</manifest>
